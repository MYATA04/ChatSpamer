HELP_COMMANDS = ("<code>/chats</code> - посмотреть список чатов (ID чата, название чата)\n"
                 "<code>/addchat</code> - добавить чат (ID чата, название чата, рассылка вступит в силу в этот чат, после рассылки во все текущие чаты)\n"
                 "<code>/delchat</code> - удалить чат (ID чата из предоставленного списка, если идет рассылка, и в этот чат еще не отправлялась рассылка, то сперва рассылка отправится и в этот чат, и после конца полной рассылки, чат удалится)\n\n"
                 "<code>/msg</code> - посмотреть последнее сообщение рассылки\n"
                 "<code>/setmsg</code> - изменить сообщение рассылки (если на данный момент идет рассылка, вступит в силу после рассылки текущего сообщения во все чаты, в ином случае, рассылка начнется сразу)\n"
                 "<code>/startmsg</code> - начать рассылку (рассылка начнется сразу)\n"
                 "<code>/stopmsg</code> - остановить рассылку (если рассылка спит, то оно остановится после спячки)\n\n"
                 "<code>/int</code> - посмотреть интервалы времени рассылки\n"
                 "<code>/intchat</code> - изменить интервал времени рассылки между чатами (в силу вступит в следующей спячке)\n"
                 "<code>/intchats</code> - изменить интервал времени рассылки после рассылки во все чаты (в силу вступит в следующей спячке)\n\n"
                 "<code>/help</code> - посмотреть команды\n\n"
                 "Чтобы отменить действия, отправьте одно из этих сообщений: <code>О</code> ; <code>о</code> ; <code>Отмена</code> ; "
                 "<code>отмена</code> ; <code>Отменить</code> ; <code>отменить</code>")

DB_PATH = "example.sqlite"